<%# The 'currentPath' variable must be passed from your route when rendering the page %>
<%# Example: res.render("index", { title: "...", currentPath: req.path }); %>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
  <div class="container">
    <a href="/<%= i18n.language %>" class="navbar-brand">
      <i class="fas fa-briefcase me-2"></i>Fernando S. Gutierrez
    </a>
    <button class="navbar-toggler" data-bs-target="#my-nav" data-bs-toggle="collapse" aria-controls="my-nav"
      aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div id="my-nav" class="collapse navbar-collapse">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a href="/<%= i18n.language %>" class="nav-link <%= (currentPath === `/${i18n.language}`) ? 'active' : '' %>"
            <%= (currentPath === `/${i18n.language}`) ? 'aria-current="page"' : '' %>>
            <i class="fas fa-home me-1"></i><%= t('nav.home') %>
          </a>
        </li>
        <li class="nav-item">
          <a href="/<%= i18n.language %>/servicios"
            class="nav-link <%= (currentPath === `/${i18n.language}/servicios`) ? 'active' : '' %>"
            <%= (currentPath === `/${i18n.language}/servicios`) ? 'aria-current="page"' : '' %>>
            <i class="fas fa-concierge-bell me-1"></i><%= t('nav.services') %>
          </a>
        </li>
        <li class="nav-item">
          <a href="/<%= i18n.language %>/casos-de-exito"
            class="nav-link <%= (currentPath === `/${i18n.language}/casos-de-exito`) ? 'active' : '' %>"
            <%= (currentPath === `/${i18n.language}/casos-de-exito`) ? 'aria-current="page"' : '' %>>
            <i class="fas fa-briefcase me-1"></i><%= t('nav.caseStudies') %>
          </a>
        </li>
        <li class="nav-item">
          <a href="/<%= i18n.language %>/contact"
            class="nav-link <%= (currentPath === `/${i18n.language}/contact`) ? 'active' : '' %>"
            <%= (currentPath === `/${i18n.language}/contact`) ? 'aria-current="page"' : '' %>>
            <i class="fas fa-envelope me-1"></i><%= t('nav.contact') %>
          </a>
        </li>
      </ul>

      <div class="navbar-nav ms-lg-3">
        <% const otherLang = i18n.language === 'es' ? 'en' : 'es' %>
        <% const otherLangText = i18n.language === 'es' ? 'English' : 'Español' %>
        <% const newPath = originalUrl.startsWith(`/${i18n.language}`) ? originalUrl.substring(3) || '/' : originalUrl %>
        <a href="/<%= otherLang %><%= newPath %>" class="nav-link"><i
            class="fas fa-globe me-1"></i><%= otherLangText %></a>
      </div>

    </div>
  </div>
</nav>